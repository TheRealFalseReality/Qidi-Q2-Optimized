# QIDI Q2 stock config snapshots

Runtime restore path: `installer/runtime/legacy_manual_install.py`.

Bundled restore roots:

- `installer/stock/qidi-q2-defaults/firmwares/01.01.02.04/config/`

Legacy manual-install reset selects the restore root by detected firmware.

Sensitive and runtime-owned files must stay sanitized when mirrored into this repository.
